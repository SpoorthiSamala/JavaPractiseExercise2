package com.stackroute.junit;

public class EvenTest {
    public static boolean isEven(int number) //to check whether a number is even
    {
        if(number%2==0)
            return true;
        else
            return false;
    }
}
